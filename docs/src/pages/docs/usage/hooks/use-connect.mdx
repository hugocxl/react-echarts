# useConnect

Connects interaction of multiple chart series.

## Usage

```js
import { useConnect } from '@kbox-labs/react-echarts'

function App() {
  const [isConnected, setIsConnected] = useConnect('group1')

  return <div onClick={() => setIsConnected(true)}>{/* ... */}</div>
}

/* 
  To disconnect the group, 
  just call "setIsConnected(false)" 
  */
```

## API

```typescript
const useConnect: (
  group: string
) => (boolean | ((connected: boolean) => void))[]
```

### Parameters

| Prop              | Description                                   |   Type    |     Default |
| :---------------- | :-------------------------------------------- | :-------: | ----------: |
| **`group`**       | Group name to be used in chart connection.    | `string`  | `undefined` |
| **`isConnected`** | Control whether the group is connected or not | `boolean` |     `false` |
