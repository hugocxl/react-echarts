<h1 className='nx-mt-12 nx-mb-12 nx-text-center nx-text-4xl nx-font-bold nx-tracking-tight nx-text-slate-900 dark:nx-text-slate-100'>
  Examples
</h1>

Some examples and use cases

## Creating your own `<EChart />` component

```tsx
export const EChart: FC<EChartProps> = props => {
  const [ref, echartsInstance] = useECharts(props)

  return <div ref={ref} />
}
```
